pipeline {
    agent any

  
    stages {
        stage('Build') {
            steps {
                git credentialsId: 'GIT_CREDS', url: 'https://github.com/pravi1991/ci-cd.git'
            }

            }
        }
    }
